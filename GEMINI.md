# Auto-Push Rule

**Always commit and push my code to GitHub after you finish implementing a request.**

- When you have completed a feature, bug fix, or UI change and verified that it works, automatically run `git add .`, `git commit -m "..."`, and `git push` before ending your turn.
- Do not wait for me to explicitly ask you to push.
